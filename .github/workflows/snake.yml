name: Generate Snake Animation

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: Platane/snk@v3
        with:
          github_user_name: toRolex
          outputs: |
            out/github-contribution-grid-snake.svg
            out/github-contribution-grid-snake-dark.svg
      - name: Replace colors with Catppuccin Mocha
        run: |
          sudo chown -R $(whoami) out/
          sed -i \
            -e 's/--c0:#161b22/--c0:#313244/' \
            -e 's/--c1:#01311f/--c1:#45475a/' \
            -e 's/--c2:#034525/--c2:#6c7086/' \
            -e 's/--c3:#0f6d31/--c3:#89b4fa/' \
            -e 's/--c4:#00c647/--c4:#cba6f7/' \
            -e 's/--ce:#161b22/--ce:#1e1e2e/' \
            -e 's/--cb:#1b1f230a/--cb:#3132440a/' \
            -e 's/purple/#cba6f7/' \
            out/github-contribution-grid-snake-dark.svg
      - uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: out
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
